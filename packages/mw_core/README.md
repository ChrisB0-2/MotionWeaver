# mw_core

The deterministic MotionWeaver compiler core. Owns the canonical `motion_spec`
data model, semantic validation, mesh/geometry analysis, the rig-agnostic
kinematic graph, and exporters. Has **no** dependency on cloud AI or Blender.
